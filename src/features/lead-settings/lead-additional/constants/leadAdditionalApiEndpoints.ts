/**
 * Lead additional-field CRUD routes.
 *
 * Used by:
 * - leadAdditionalService (lead-settings/lead-additional page)
 */
export enum LEAD_ADDITIONAL_API_ENDPOINTS {
  ADDITIONAL_FIELDS = '/lead-settings/additional-fields',
}

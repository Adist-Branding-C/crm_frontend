export interface CreateLeadStatusPayload {
  status: string;
  color: string;
  conversion: boolean;
}

export interface UpdateLeadStatusPayload {
  status: string;
  color: string;
  conversion: boolean;
}

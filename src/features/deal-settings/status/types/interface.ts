export interface DealStatusItem {
  id: number;
  name: string;
  stage: string;
  status: boolean;
}

export interface ApiDealStatusItem {
  id: number;
  name: string;
  stage: string;
  status: boolean;
}

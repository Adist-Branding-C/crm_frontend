/**
 * Company CRUD route prefixes, used to build the actual endpoint URLs in
 * constants/companiesApiEndpoints.ts and called directly by companyDataService.
 */
export enum COMPANIES_API_ENDPOINTS {
  COMPANIES = '/companies',
  STATISTICS = '/companies/statistics',
}

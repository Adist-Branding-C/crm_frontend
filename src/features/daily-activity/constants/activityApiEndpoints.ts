/**
 * Activity feature API endpoint paths.
 *
 * Used by:
 * - ActivityService (getActivities, getStaffOptions).
 */
export enum ACTIVITY_API_ENDPOINTS {
  ACTIVITIES = '/activities',
  STAFF = '/staff',
}

export interface DealAdditionalFieldDef {
  fieldId: string;
  fieldName: string;
  fieldKey: string;
  fieldType: string;
  isRequired: boolean;
  showInList: boolean;
  showInFilter: boolean;
  values: string[];
}

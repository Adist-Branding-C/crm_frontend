export enum PIPELINE_API_ENDPOINTS  {
  LEADS= '/pipeline/leads',
  DEALS= '/pipeline/deals',
  TASKS= '/pipeline/tasks',
  STATUS_DEALS= '/pipeline/statusbydeals',
  STATUS_LEADS= '/pipeline/statusbyleads',
  STATUS_TASKS= '/pipeline/statusbytask',
};
